# Pinned deliberately. The MCP SDK's v2 line moved from decorator-based handler
# registration to constructor-based, and now diverts fd 1 inside stdio_server()
# itself. Task 1 targets the 1.x API, which is what the published docs and most
# reviewers know, and implements the stdout guard explicitly.
mcp>=1.9,<2
pydantic>=2.7

fastapi>=0.115
uvicorn>=0.30
httpx>=0.27

pytest>=8.0
pytest-asyncio>=0.24
